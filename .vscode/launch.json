{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "gdb",
            "request": "attach",
            "name": "Attach to kernel",
            "executable": "${workspaceRoot}/build/rros.bin",
            "target": "localhost:1234",
            "remote": true,
            "cwd": "${workspaceRoot}", 
            "gdbpath": "/usr/bin/gdb",
            "preLaunchTask": "Build bin-file",
        },
    ]
}